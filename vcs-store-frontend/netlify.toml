[build]
  command = "npm run build"
  publish = "dist/vcs-store-front/browser"

[context.branch-deploy]
  command = "npm run build:qa"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
