services:
  backend:
    command:
      - uvicorn
      - app.main:app
      - --host
      - "0.0.0.0"
      - --port
      - "8000"
      - --reload
    volumes:
      - ./vcs-store-backend:/app

  frontend:
    build:
      args:
        BUILD_CONFIG: development
